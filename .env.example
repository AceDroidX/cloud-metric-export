# 至少需要配置一个数据源（64clouds 或 AWS）

# 64clouds 配置（可选）
CLOUD_64CLOUDS_VEID=
CLOUD_64CLOUDS_API_KEY=
# CLOUD_64CLOUDS_FETCH_INTERVAL=60

# AWS 配置（可选）
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-northeast-1
AWS_LIGHTSAIL_INSTANCE_NAME=
# AWS_LIGHTSAIL_FETCH_INTERVAL=300

# 可选配置
# METRICS_PORT=9090
